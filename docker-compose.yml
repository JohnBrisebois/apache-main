services:
  apache:
    image: httpd:latest
    container_name: johnbrisebois.com
    expose:
      - '80'
    networks:
      - webnet
    volumes:
      - ./public-html:/usr/local/apache2/htdocs/

networks:
  webnet:
    external: true
